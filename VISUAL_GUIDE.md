# Visual Guide - Theme System Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         User Interface                          │
│                                                                 │
│  ┌──────────────────┐              ┌──────────────────────┐   │
│  │  ThemeSwitcher   │              │ AdvancedThemeSwitcher│   │
│  │                  │              │                      │   │
│  │  [Dropdown]      │              │  [Modal Dialog]      │   │
│  │  [Mode Toggle]   │              │  [Search & Filter]   │   │
│  └────────┬─────────┘              └──────────┬───────────┘   │
│           │                                   │               │
└───────────┼───────────────────────────────────┼───────────────┘
            │                                   │
            └───────────────┬───────────────────┘
                            │
                ┌───────────▼────────────┐
                │   useThemeConfig()     │
                │   Custom Hook          │
                │                        │
                │  - customTheme         │
                │  - setCustomTheme      │
                │  - isDark              │
                │  - toggleMode          │
                └───────────┬────────────┘
                            │
            ┌───────────────┴────────────────┐
            │                                │
    ┌───────▼────────┐            ┌─────────▼─────────┐
    │  ThemeContext  │            │   next-themes     │
    │  (Custom)      │            │   ThemeProvider   │
    │                │            │                   │
    │  - theme name  │            │  - light/dark     │
    │  - CSS file    │            │  - system theme   │
    └───────┬────────┘            └─────────┬─────────┘
            │                               │
            │ Manages CSS Loading           │ Manages Classes
            │                               │
    ┌───────▼────────┐            ┌─────────▼─────────┐
    │  <link> tag    │            │  <html> class     │
    │  Dynamic       │            │  .dark or .light  │
    │  injection     │            │                   │
    └───────┬────────┘            └─────────┬─────────┘
            │                               │
            └───────────────┬───────────────┘
                            │
                ┌───────────▼────────────┐
                │    Browser Rendering   │
                │                        │
                │  Applies CSS variables │
                │  from theme + mode     │
                └────────────────────────┘
```

## Data Flow - Theme Selection

```
User clicks theme
      │
      ▼
┌─────────────────┐
│ ThemeSwitcher   │
│ calls           │
│ setCustomTheme()│
└────────┬────────┘
         │
         ▼
┌──────────────────────┐
│ ThemeContext         │
│ updates state        │
│ saves to localStorage│
└────────┬─────────────┘
         │
         ▼
┌──────────────────────┐
│ useEffect triggers   │
│ in ThemeProvider     │
└────────┬─────────────┘
         │
         ├─────────────────┐
         │                 │
         ▼                 ▼
┌────────────────┐  ┌──────────────┐
│ Remove old     │  │ Create new   │
│ <link> tag     │  │ <link> tag   │
└────────┬───────┘  └──────┬───────┘
         │                 │
         └────────┬────────┘
                  │
                  ▼
         ┌────────────────┐
         │ Browser loads  │
         │ new CSS file   │
         └────────┬───────┘
                  │
                  ▼
         ┌────────────────┐
         │ Theme applied! │
         └────────────────┘
```

## File Structure Diagram

```
next-js-bolt-boilerplate/
│
├── app/
│   ├── layout.tsx ──────────► Wraps app with ThemeProvider
│   ├── page.tsx ────────────► Demo page with components
│   └── globals.css ─────────► Base styles + CSS variables
│
├── components/
│   ├── providers/
│   │   └── theme-provider.tsx ──► Core theme system
│   │
│   ├── theme-switcher.tsx ─────► Simple UI
│   ├── advanced-theme-switcher.tsx ──► Advanced UI
│   │
│   └── ui/ ────────────────────► shadcn components
│       ├── button.tsx
│       ├── card.tsx
│       ├── select.tsx
│       └── ... (40+ components)
│
├── hooks/
│   └── use-theme-config.ts ───► Unified theme hook
│
├── lib/
│   ├── themes.ts ──────────────► Theme configuration
│   └── utils.ts ───────────────► Utilities (cn, etc.)
│
└── public/
    └── themes/ ────────────────► CSS theme files
        ├── default.css ───────► 7KB
        ├── cyberpunk.css ─────► 7KB
        ├── claude.css ────────► 7KB
        └── ... (47+ more) ────► ~7KB each
                                  Total: ~350KB
                                  (Only 1 loaded at a time!)
```

## Component Architecture

```
┌────────────────────────────────────────────────────┐
│                   RootLayout                       │
│                                                    │
│  <html suppressHydrationWarning>                  │
│    <body>                                          │
│      ┌──────────────────────────────────────┐    │
│      │      ThemeProvider                    │    │
│      │  ┌────────────────────────────────┐  │    │
│      │  │   NextThemesProvider           │  │    │
│      │  │                                │  │    │
│      │  │  ┌──────────────────────────┐ │  │    │
│      │  │  │   ThemeContext.Provider  │ │  │    │
│      │  │  │                          │ │  │    │
│      │  │  │   {children}            │ │  │    │
│      │  │  │     │                   │ │  │    │
│      │  │  │     ├─ Header          │ │  │    │
│      │  │  │     │    └─ ThemeSwitcher │ │  │    │
│      │  │  │     │                   │ │  │    │
│      │  │  │     ├─ Main Content    │ │  │    │
│      │  │  │     │                   │ │  │    │
│      │  │  │     └─ Footer          │ │  │    │
│      │  │  └──────────────────────────┘ │  │    │
│      │  └────────────────────────────────┘  │    │
│      └──────────────────────────────────────┘    │
│    </body>                                        │
│  </html>                                          │
└────────────────────────────────────────────────────┘
```

## Theme CSS Structure

```
┌────────────────────────────────────────────────────┐
│              theme-name.css                        │
│                                                    │
│  :root {                                          │
│    ┌────────────────────────────────────────┐    │
│    │  Light Mode Variables                  │    │
│    │                                         │    │
│    │  --background: oklch(...)              │    │
│    │  --foreground: oklch(...)              │    │
│    │  --primary: oklch(...)                 │    │
│    │  --secondary: oklch(...)               │    │
│    │  ... (30+ variables)                   │    │
│    │                                         │    │
│    │  --font-sans: "Font Name", sans-serif  │    │
│    │  --radius: 0.5rem                      │    │
│    │  --shadow-*: ...                       │    │
│    └────────────────────────────────────────┘    │
│  }                                                │
│                                                    │
│  .dark {                                          │
│    ┌────────────────────────────────────────┐    │
│    │  Dark Mode Variables                   │    │
│    │                                         │    │
│    │  --background: oklch(...)              │    │
│    │  --foreground: oklch(...)              │    │
│    │  --primary: oklch(...)                 │    │
│    │  ... (overrides for dark mode)         │    │
│    └────────────────────────────────────────┘    │
│  }                                                │
│                                                    │
│  @theme inline {                                  │
│    ┌────────────────────────────────────────┐    │
│    │  Tailwind Integration                  │    │
│    │                                         │    │
│    │  --color-background: var(--background) │    │
│    │  --color-primary: var(--primary)       │    │
│    │  --font-sans: var(--font-sans)         │    │
│    │  ... (Tailwind mappings)               │    │
│    └────────────────────────────────────────┘    │
│  }                                                │
└────────────────────────────────────────────────────┘
```

## State Management Flow

```
┌─────────────────────────────────────────────────────────┐
│                    User Action                          │
└────────────┬────────────────────────────────────────────┘
             │
    ┌────────┴─────────┐
    │                  │
    ▼                  ▼
┌───────────┐    ┌──────────────┐
│  Theme    │    │  Dark/Light  │
│  Change   │    │  Mode Toggle │
└─────┬─────┘    └──────┬───────┘
      │                 │
      ▼                 ▼
┌───────────────┐  ┌──────────────────┐
│ setCustomTheme│  │ setTheme (next-  │
│               │  │ themes)          │
└──────┬────────┘  └────────┬─────────┘
       │                    │
       ▼                    ▼
┌─────────────────┐  ┌──────────────────┐
│ localStorage    │  │ localStorage     │
│ 'custom-theme'  │  │ 'theme'          │
└──────┬──────────┘  └────────┬─────────┘
       │                      │
       ▼                      ▼
┌─────────────────┐  ┌──────────────────┐
│ Update <link>   │  │ Update <html>    │
│ href="/themes/  │  │ class="dark" or  │
│ name.css"       │  │ class="light"    │
└──────┬──────────┘  └────────┬─────────┘
       │                      │
       └──────────┬───────────┘
                  │
                  ▼
         ┌────────────────┐
         │ Browser Re-    │
         │ renders with   │
         │ new styles     │
         └────────────────┘
```

## CSS Variable Resolution

```
Component needs color
         │
         ▼
Uses Tailwind class: "bg-primary"
         │
         ▼
Tailwind resolves to: var(--color-primary)
         │
         ▼
@theme inline maps: --color-primary → var(--primary)
         │
         ▼
Theme CSS defines: --primary: oklch(...)
         │
         ├──── Light mode: :root { --primary: ... }
         │
         └──── Dark mode: .dark { --primary: ... }
         │
         ▼
Browser applies final color value
```

## Theme Switcher UI Components

```
┌─────────────────────────────────────────────────┐
│            Basic ThemeSwitcher                  │
│                                                 │
│  ┌──────────────────────┐  ┌─────────────┐    │
│  │  [🎨 Theme Name  ▼] │  │  [☀️/🌙]   │    │
│  │                      │  │             │    │
│  │  Select Dropdown     │  │ Mode Toggle │    │
│  └──────────────────────┘  └─────────────┘    │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│         Advanced ThemeSwitcher                  │
│                                                 │
│  ┌─────────────┐                               │
│  │ [🎨 Theme] │ ← Button opens modal           │
│  └─────────────┘                               │
│                                                 │
│  Opens ▼                                        │
│                                                 │
│  ┌───────────────────────────────────────────┐ │
│  │  Choose Your Theme                        │ │
│  │                                           │ │
│  │  Search: [__________________]            │ │
│  │                                           │ │
│  │  [☀️ Light] [🌙 Dark]                     │ │
│  │                                           │ │
│  │  Categories: [All][Military][Tech]...    │ │
│  │                                           │ │
│  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐   │ │
│  │  │Theme1│ │Theme2│ │Theme3│ │Theme4│   │ │
│  │  │ ✓    │ │      │ │      │ │      │   │ │
│  │  └──────┘ └──────┘ └──────┘ └──────┘   │ │
│  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐   │ │
│  │  │Theme5│ │Theme6│ │Theme7│ │Theme8│   │ │
│  │  └──────┘ └──────┘ └──────┘ └──────┘   │ │
│  │  ...                                     │ │
│  └───────────────────────────────────────────┘ │
└─────────────────────────────────────────────────┘
```

## Performance Comparison

```
Traditional Approach (All CSS loaded):
┌────────┬────────┬────────┬─────┬────────┐
│Theme 1 │Theme 2 │Theme 3 │ ... │Theme 50│
│  7KB   │  7KB   │  7KB   │ ... │  7KB   │
└────────┴────────┴────────┴─────┴────────┘
Total Bundle: ~350KB ❌

Our Approach (Dynamic Loading):
┌────────┐
│Theme X │  ← Only loaded theme
│  7KB   │
└────────┘
Total Bundle: ~7KB ✅

Savings: 98% reduction! 🎉
```

## User Journey

```
1. User visits site
   │
   ▼
2. Default theme loads (7KB CSS)
   │
   ▼
3. User opens theme switcher
   │
   ▼
4. Sees 50+ themes in organized categories
   │
   ▼
5. Searches or browses themes
   │
   ▼
6. Clicks "Cyberpunk"
   │
   ▼
7. Theme instantly switches (<100ms)
   │
   ▼
8. Preference saved to localStorage
   │
   ▼
9. Returns tomorrow → Cyberpunk still active! ✨
   │
   ▼
10. Toggles to dark mode
    │
    ▼
11. Dark Cyberpunk theme applied! 🌙
```

## Integration Points

```
Your Application
├── Uses shadcn/ui components
│   └── Automatically themed via CSS variables
│
├── Custom components
│   └── Use Tailwind classes (bg-primary, text-foreground)
│       └── Automatically themed!
│
└── Direct CSS
    └── Use var(--background), var(--primary), etc.
        └── Automatically themed!

Everything just works! ✨
```

## Legend

```
┌─────┐   Component/Container
│     │
└─────┘

  │      Data/control flow
  ▼

  ├──    Branch/decision
  └──

  ►      Action/process

 [...]   Interactive element

  ✓      Selected/active state
  ✅     Success/recommended
  ❌     Not recommended
  ⚠️      Warning/caution
  ✨     Special feature
```

---

This visual guide complements the written documentation with diagrams showing how the theme system works at a glance.

